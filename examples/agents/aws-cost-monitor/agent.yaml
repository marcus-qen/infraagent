apiVersion: legator.io/v1alpha1
kind: LegatorAgent
metadata:
  name: aws-cost-monitor
spec:
  description: "Monitors AWS spending, identifies cost anomalies, and recommends savings."
  modelTier: standard
  schedule:
    cron: "0 6 * * *"  # Daily at 6 AM
  guardrails:
    autonomy: observe
    maxIterations: 8
    tokenBudget: 30000
  skill:
    source: "configmap://aws-cost-monitor-skill"
  environment: aws-production
