{{- /*
Per-agent ServiceAccount and RBAC template.
Users apply this per InfraAgent by setting agents[] in values.
Each agent gets a dedicated ServiceAccount with scoped Role/RoleBinding
limited to the namespaces declared in its environment.

Usage in values.yaml:
  agents:
    - name: watchman-light
      namespace: infraagent
      targetNamespaces:
        - backstage
        - monitoring
        - argocd
*/ -}}
{{- range $agent := .Values.agents }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: infraagent-{{ $agent.name }}
  namespace: {{ $agent.namespace | default $.Release.Namespace }}
  labels:
    {{- include "infraagent.labels" $ | nindent 4 }}
    infraagent.io/agent: {{ $agent.name }}
{{- range $ns := $agent.targetNamespaces }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: infraagent-{{ $agent.name }}-reader
  namespace: {{ $ns }}
  labels:
    {{- include "infraagent.labels" $ | nindent 4 }}
    infraagent.io/agent: {{ $agent.name }}
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "endpoints", "configmaps", "events", "persistentvolumeclaims"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "replicasets", "daemonsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses", "networkpolicies"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: infraagent-{{ $agent.name }}-reader
  namespace: {{ $ns }}
  labels:
    {{- include "infraagent.labels" $ | nindent 4 }}
    infraagent.io/agent: {{ $agent.name }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: infraagent-{{ $agent.name }}-reader
subjects:
  - kind: ServiceAccount
    name: infraagent-{{ $agent.name }}
    namespace: {{ $agent.namespace | default $.Release.Namespace }}
{{- end }}
{{- end }}
